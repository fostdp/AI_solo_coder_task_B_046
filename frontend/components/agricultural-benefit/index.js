/**
 * 农业受益区渲染组件 - 入口文件
 * 导出 BenefitRenderer 类
 *
 * 使用方式：
 *   const renderer = new BenefitRenderer(map, options);
 *   renderer.setBenefitZones(zones);
 *   renderer.show();
 */

if (typeof module !== 'undefined' && module.exports) {
    module.exports = { BenefitRenderer };
}
