/**
 * 网络效应图层组件 - 入口文件
 * 导出 NetworkLayer 类
 *
 * 使用方式：
 *   const networkLayer = new NetworkLayer(options);
 *   networkLayer.addTo(map);
 *   networkLayer.loadNetwork(region);
 */

if (typeof module !== 'undefined' && module.exports) {
    module.exports = { NetworkLayer };
}
