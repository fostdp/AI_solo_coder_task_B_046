/**
 * 数字化展示面板组件 - 入口文件
 * 导出 DigitalExhibitPanel 类
 *
 * 使用方式：
 *   const panel = new DigitalExhibitPanel(options);
 *   panel.init(sitePanelEl);
 *   panel.loadSite(siteId);
 */

if (typeof module !== 'undefined' && module.exports) {
    module.exports = { DigitalExhibitPanel, RECONSTRUCTION_STEPS };
}
